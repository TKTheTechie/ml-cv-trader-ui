import './Header.svelte.css.proxy.js';
/* src/lib/header/Header.svelte generated by Svelte v3.46.4 */
import {
	SvelteComponent,
	append,
	attr,
	create_component,
	destroy_component,
	detach,
	element,
	init,
	insert,
	mount_component,
	noop,
	safe_not_equal,
	space,
	transition_in,
	transition_out
} from "../../../_snowpack/pkg/svelte/internal.js";

import Icon from '../../../_snowpack/pkg/svelte-awesome.js';
import { github } from '../../../_snowpack/pkg/svelte-awesome/icons.js';

function create_fragment(ctx) {
	let header;
	let section;
	let div;
	let span;
	let t1;
	let nav;
	let a;
	let icon;
	let current;
	icon = new Icon({ props: { data: github, scale: 2 } });

	return {
		c() {
			header = element("header");
			section = element("section");
			div = element("div");
			span = element("span");
			span.textContent = "ml-cv-trader";
			t1 = space();
			nav = element("nav");
			a = element("a");
			create_component(icon.$$.fragment);
			attr(span, "class", "relative header-text z-10 flex items-center w-auto text-2xl font-extrabold leading-none text-green-500 select-none svelte-m76vwu");
			attr(a, "href", "https://github.com/TKTheTechie/cv-ml-trader-ui");
			attr(a, "class", "relative text-lg leading-6 dark:text-white transition duration-150 ease-out hover:text-slate-500");
			attr(a, "target", "_blank");
			attr(nav, "class", "top-0 left-0 z-0 flex items-center justify-center w-full h-full py-5 -ml-0 space-x-5 text-base md:-ml-5 md:py-0 md:absolute");
			attr(div, "class", "container flex flex-col flex-wrap items-center justify-between py-3 mx-auto md:flex-row max-w-7xl");
			attr(section, "class", "relative w-full px-8 text-gray-700 dark:bg-slate-900 bg-white body-font");
		},
		m(target, anchor) {
			insert(target, header, anchor);
			append(header, section);
			append(section, div);
			append(div, span);
			append(div, t1);
			append(div, nav);
			append(nav, a);
			mount_component(icon, a, null);
			current = true;
		},
		p: noop,
		i(local) {
			if (current) return;
			transition_in(icon.$$.fragment, local);
			current = true;
		},
		o(local) {
			transition_out(icon.$$.fragment, local);
			current = false;
		},
		d(detaching) {
			if (detaching) detach(header);
			destroy_component(icon);
		}
	};
}

class Header extends SvelteComponent {
	constructor(options) {
		super();
		init(this, options, null, create_fragment, safe_not_equal, {});
	}
}

export default Header;